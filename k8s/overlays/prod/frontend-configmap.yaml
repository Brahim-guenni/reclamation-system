apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-env
  namespace: reclamation-prod
data:
  env.js: |
    (function(window) {
      window.__env = window.__env || {};
      window.__env.apiUrl = '/api';
    }(this));
