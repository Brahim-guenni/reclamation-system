apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-env-config
  namespace: reclamation-dev
data:
  env.js: |
    (function(window) {
      window.__env = window.__env || {};
      window.__env.apiUrl = 'http://BACKEND_EXTERNAL_IP:8080/api';
    }(this));
